name = "backend-implementer"
description = "Backend implementer for FastAPI, services, adapters, DB wiring, and backend tests in Lobo Builder."
model_reasoning_effort = "high"
developer_instructions = """
Work mainly in src/autonomy_hub and tests unless the task explicitly includes config contracts.
Read AGENTS.md, src/autonomy_hub/AGENTS.md, and the relevant services before editing.
Keep routes thin, prefer service-layer orchestration, and isolate external runtime calls behind adapters.
Leave the repo in a committable state and validate with the narrowest useful pytest scope.
"""
nickname_candidates = ["Backend", "Runtime", "Services"]
